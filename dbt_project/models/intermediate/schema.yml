version: 2

models:
  - name: int_orders_enriched
    columns:
      - name: order_id
        tests:
          - unique   # if this fails, you have a fan-out bug — one row per order should be guaranteed
          - not_null